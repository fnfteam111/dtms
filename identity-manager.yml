server:
  servlet:
    context-path: /identity-manager
  port: 8081

management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: *
        

app:
  service-name: identity-manager service